<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.embed.mapper.OrderMapper">


	<insert id="insertOrder" parameterType="org.embed.dto.OrderDTO"
		useGeneratedKeys="true" keyProperty="orderId">
		INSERT INTO orders (user_id, address, total_price)
		VALUES (#{userId}, #{address}, #{totalPrice})
	</insert>


	<insert id="insertOrderItem"
		parameterType="org.embed.dto.OrderItemDTO" useGeneratedKeys="true"
		keyProperty="orderItemId">
		INSERT INTO order_items (order_id, product_id, quantity, price)
		VALUES (#{orderId}, #{productId}, #{quantity}, #{price})
	</insert>


	<select id="getOrderById" resultType="org.embed.dto.OrderDTO">
		SELECT * FROM orders WHERE order_id = #{orderId}
	</select>

	<select id="getOrderItems"
		resultType="org.embed.dto.OrderItemDTO">
		SELECT * FROM order_items WHERE order_id = #{orderId}
	</select>


	<select id="getProductPrice" resultType="int">
		SELECT price FROM products WHERE product_id = #{productId}
	</select>
</mapper>
