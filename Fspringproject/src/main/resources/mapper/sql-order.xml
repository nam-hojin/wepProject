<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.embed.mapper.OrderMapper">

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO orders (user_id, recipient_name, address, phone, total_price, order_date)
        VALUES (#{userId}, #{recipientName}, #{address}, #{phone}, #{totalPrice}, #{orderDate})
    </insert>

    <insert id="insertOrderItem" useGeneratedKeys="true" keyProperty="orderItemId">
        INSERT INTO order_items (order_id, product_id, quantity, price, created_at, updated_at)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{price}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>
